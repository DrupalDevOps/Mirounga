# Barebones Nginx test config file for HTML only.

server {

    listen 80;
    root /www/localenv/build/nginx/test/html; # Set in docker compose volumes.
    server_name localhost;

    index index.html index.php;

}
