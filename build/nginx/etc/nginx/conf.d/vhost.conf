server {

    # Redirect non-https traffic to https
    # if ($scheme != "https") {
    #   return 301 https://$host$request_uri;
    #   # return 301 https://alexanderallen.name$request_uri;
    # }

    server_name alexanderallen.name;
    # server_name _ "";
    listen 443 ssl;
    # listen 8080;
    # ssl off;
    # return 444;
    # ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
    # ssl_prefer_server_ciphers on;

    # ssl_dhparam /vsdroot/letsencrypt/ssl-dhparams.pem;



    ssl_certificate /vsdroot/letsencrypt/live/alexanderallen.name/fullchain.pem;
    ssl_certificate_key /vsdroot/letsencrypt/live/alexanderallen.name/privkey.pem;

    # ssl_dhparam /vsdroot/letsencrypt/ssl-dhparams.pem;
  # include /vsdroot/letsencrypt/options-ssl-nginx.conf;

    # add_header Strict-Transport-Security "max-age=31536000; includeSubDomains";



    # Moving root outside of location block provides fallback, avoiding noroot side effects.
    # root /usr/share/nginx/html;
    # root "${projectRoot}";
    root "/vsdroot";

    # Set default access log, log most requests to container filesystem.
    # Only surface relevant/important requests to /dev/stdout on Docker logs.
    access_log /home/nobody/supplemental.log supplemental;

    location ~ favicon.ico$ {
        log_not_found off;
        access_log off;
    }

    location / {
        index index.html;
    }

    # location / {
    #     try_files $uri @php/;
    # }

    # location @rewrite {
    #     rewrite ^ /index.php; # For Drupal >= 7
    # }

    # location ~ '\.php$|^/update.php' {
    #     fastcgi_split_path_info ^(.+?\.php)(|/.*)$;
    #     include fastcgi.conf;
    #     fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    #     fastcgi_param PATH_INFO $fastcgi_path_info;
    #     fastcgi_param QUERY_STRING $query_string;
    #     fastcgi_intercept_errors on;

    #     fastcgi_pass unix:/sock/docker.sock;
    #     access_log  /dev/stdout docker;
    # }

    # location @php/ {
    #     fastcgi_index index.php;

    #     fastcgi_split_path_info ^(.+?\.php)(|/.*)$;
    #     include fastcgi.conf;
    #     fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    #     fastcgi_param PATH_INFO $fastcgi_path_info;
    #     fastcgi_param QUERY_STRING $query_string;
    #     fastcgi_intercept_errors on;

    #     fastcgi_pass unix:/sock/docker.sock;
    #     access_log  /dev/stdout docker;
    # }

    # location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
    #     try_files $uri @rewrite;
    #     expires max;
    #     log_not_found off;
    # }
    # location ~ ^/sites/.*/files/styles/ { # For Drupal >= 7
    #     try_files $uri @rewrite;
    # }
    # # location ~ ^(/[a-z\-]+)?/system/files/ { # For Drupal >= 7
    # #     try_files $uri /index.php?$query_string;
    # # }
    # # TODO: test this.
    # location ~ ^(/[a-z\-]+)?/system/files/ { # For Drupal >= 7
    #     try_files $uri @php/;
    # }
    # if ($request_uri ~* "^(.*/)index\.php/(.*)") {
    #     return 307 $1$2;
    # }

}
