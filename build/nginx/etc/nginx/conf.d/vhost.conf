server {

    listen 8080;

    # server_name localhost;

    # Don't need to specify root in server.
    # root /app;

    # server_name _;

    # Directive server_name can only be used in server context.
    # Allows capturing regex group and passing to location context.
    # http://nginx.org/en/docs/http/ngx_http_core_module.html#server_name
    # http://nginx.org/en/docs/http/server_names.html
    #
    # Supports both classic numbered ($1, $2) captures, as well as named (<name>) captures.

    # Capture the whole hostname, should match a directory.
    # /etc/hosts still needs to be updated...
    # Open elevated cmd.exe shell and type: notepad.exe C:\Windows\System32\Drivers\etc\hosts
    server_name ~^(?<domaincapture>.*)$;


    # this worked here for default location.
    # location / {
    #     root /sites/default;
    #     index index.html;
    # }

    #root '/sites';


    location / {
        #root /sites/$domaincapture;
        index index.html;
    }



    location ~ '\.php$' {
        # root /sites/$domaincapture;
        # fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME /sites/$domaincapture/index.php;


        fastcgi_pass unix:/sock/docker.sock;

        access_log  /dev/stdout docker;
    }

    # location / {
    #     # try_files $uri @rewrite; # For Drupal <= 6
    #     try_files $uri /index.php?$query_string; # For Drupal >= 7
    # }

}


# Attempt a default fallback location.
server {
    listen 8080;
    server_name localhost;
    location / {
        root /sites/default;
        index index.html;
    }
}
